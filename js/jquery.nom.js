// # jQuery.nom
//
// jQuery.nom (nom) captures the DOM elements as text or blob along with a screenshot.
// This information can be sent to the server to enable better user feedback.
//
// Goal:
(function( $ ) {
  $.fn.nom = function() {

    return this; // Return the jQuery object passed into the function, to maintain extensibility
  };
})( jquery );
